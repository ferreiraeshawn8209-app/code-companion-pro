workflows:
  android-apk-debug:
    name: Spok Android APK
    # Codemagic's individual free allowance applies to macOS M2 builds.
    # Linux builders require billing to be enabled.
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      node: 20
    scripts:
      - name: Install Bun
        script: |
          curl -fsSL https://bun.sh/install | bash
          export PATH="$HOME/.bun/bin:$PATH"
          echo "$HOME/.bun/bin" >> $CM_PATHS_FILE
          bun --version
      - name: Install dependencies
        script: |
          export PATH="$HOME/.bun/bin:$PATH"
          bun install
      - name: Build web app
        script: |
          export PATH="$HOME/.bun/bin:$PATH"
          bun run build
      - name: Add Android platform
        script: |
          export PATH="$HOME/.bun/bin:$PATH"
          bunx cap add android || true
      - name: Sync Capacitor
        script: |
          export PATH="$HOME/.bun/bin:$PATH"
          bunx cap sync android
      - name: Build APK
        script: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug
    artifacts:
      - android/app/build/outputs/apk/debug/*.apk
